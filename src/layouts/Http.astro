---
import { ViewTransitions } from 'astro:transitions';


interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en" transition:animate="none">
	<head>
		<meta charset="UTF-8" />
		<meta 
			name="description" 
			content=`Descubre la elegancia y calidez de nuestras prendas de alta calidad, 
        confeccionadas con la suave y lujosa lana de alpaca. En The Alpaca Factory, 
        ofrecemos una exclusiva selección de ropa fina, diseñada para elevar tu estilo y 
        brindarte el máximo confort en cada ocasión. Explora nuestra colección de 
        prendas premium y sumérgete en la exquisita artesanía de la moda tejida 
        con amor y cuidado.` 
		/>
		<meta name="viewport" content="width=device-width" />
		<link rel="icon"  href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<ViewTransitions />
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	html {
    font-family: "Open Sans", sans-serif;
    scrollbar-color: rgb(194 65 12) black;
    scrollbar-width: 1px;
	}
	
	body {
	  margin: 0;
	}
</style>

<script>
	let prevTitle = document.title;
	window.addEventListener('blur', () => {
		prevTitle = document.title;
		document.title = '¡Por qué te vas! ¡Vuelve! 🙁';
	})
	window.addEventListener('focus', () => {
		document.title = prevTitle;
	})
</script>

